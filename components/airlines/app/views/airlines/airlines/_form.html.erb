<%= form_with(model: airline, url: (airline.persisted? ? airlines.airline_path(airline.id) : airlines.airlines_path), local: true) do |form| %>
  <% if airline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(airline.errors.count, "error") %> prohibited this airline from being saved:</h2>

      <ul>
      <% airline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
